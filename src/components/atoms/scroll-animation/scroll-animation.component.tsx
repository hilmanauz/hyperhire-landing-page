'use client';

import ReactAnimateOnScroll, { ScrollAnimationProps } from 'react-animate-on-scroll';

export const ScrollAnimation = (props: ScrollAnimationProps) => <ReactAnimateOnScroll {...props} />;
